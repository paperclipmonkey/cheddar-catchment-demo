encoding  utf-8
source "NextDoor.th"

export model -fmt survex -o "NextDoor-th.3d"

# export map -proj plan -layout local -o "NextDoorP.pdf"

# export map -proj extended -layout local -o "NextDoorEEpdf"
export map -layout local -proj [elevation 320] -o NextDoorE320.pdf


layout local
  debug off
  map-header 0 0 off
  # symbol-hide group cave-centreline
endlayout
